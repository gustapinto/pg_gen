dsn: "postgresql://example_db_user:example_db_passw@localhost:5432/example_db?sslmode=disable"
schemas:
  public:
    include_views: true
    go:
      dest: "./gen"
      package: "gen"
      emit_json_tags: true
